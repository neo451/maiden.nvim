lib.formatters
Module lib.formatters

Formatters

Functions

  ------------------------------------------------------ -----------------------------------
  Formatters.format_freq_raw (freq)                      format_freq_raw
  Formatters.format_secs_raw (secs)                      format_secs_raw
  Formatters.default (param)                             default
  Formatters.percentage (param)                          percentage
  Formatters.unipolar_as_percentage (param)              unipolar_as_percentage
  Formatters.bipolar_as_percentage (param)               bipolar_as_percentage
  Formatters.secs_as_ms (param)                          secs_as_ms
  Formatters.unipolar_as_true_false (param)              unipolar_as_true_false
  Formatters.unipolar_as_enabled_disabled (param)        unipolar_as_enabled_disabled
  Formatters.bipolar_as_pan_widget (param)               bipolar_as_pan_widget
  Formatters.unipolar_as_multimode_filter_freq (param)   unipolar_as_multimode_filter_freq
  Formatters.round (precision)                           round
  Formatters.format_freq (param)                         format_freq
  Formatters.format_secs (param)                         format_secs
  ------------------------------------------------------ -----------------------------------

Functions

 Formatters.format_freq_raw (freq)

    format_freq_raw

    Parameters:

    -   freq

 Formatters.format_secs_raw (secs)

    format_secs_raw

    Parameters:

    -   secs

 Formatters.default (param)

    default

    Parameters:

    -   param

 Formatters.percentage (param)

    percentage

    Parameters:

    -   param

 Formatters.unipolar_as_percentage (param)

    unipolar_as_percentage

    Parameters:

    -   param

 Formatters.bipolar_as_percentage (param)

    bipolar_as_percentage

    Parameters:

    -   param

 Formatters.secs_as_ms (param)

    secs_as_ms

    Parameters:

    -   param

 Formatters.unipolar_as_true_false (param)

    unipolar_as_true_false

    Parameters:

    -   param

 Formatters.unipolar_as_enabled_disabled (param)

    unipolar_as_enabled_disabled

    Parameters:

    -   param

 Formatters.bipolar_as_pan_widget (param)

    bipolar_as_pan_widget

    Parameters:

    -   param

 Formatters.unipolar_as_multimode_filter_freq (param)

    unipolar_as_multimode_filter_freq

    Parameters:

    -   param

 Formatters.round (precision)

    round

    Parameters:

    -   precision

 Formatters.format_freq (param)

    format_freq

    Parameters:

    -   param

 Formatters.format_secs (param)

    format_secs

    Parameters:

    -   param

generated by LDoc 1.4.6 Last updated 2023-03-17 18:54:24
vim:tw=78:ts=8:ft=help:norl:
