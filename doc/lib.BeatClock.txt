lib.BeatClock
Module lib.BeatClock

BeatClock

Functions

  ------------------------------ ---------------------
  new (name)                     constructor
  start (dev_id)                 start
  stop (dev_id)                  stop
  advance_step ()                advance step
  tick (dev_id)                  tick
  reset (dev_id)                 reset
  clock_source_change (source)   clock source change
  bpm_change (bpm)               bpm change
  add_clock_params ()            add clock params
  enable_midi ()                 enable midi
  process_midi (data)            process midi
  ------------------------------ ---------------------

Functions

 new (name)

    constructor

    Parameters:

    -   name string

    Returns:

 start (dev_id)

    start

    Parameters:

    -   dev_id

 stop (dev_id)

    stop

    Parameters:

    -   dev_id

 advance_step ()
    advance step

 tick (dev_id)

    tick

    Parameters:

    -   dev_id

 reset (dev_id)

    reset

    Parameters:

    -   dev_id

 clock_source_change (source)

    clock source change

    Parameters:

    -   source

 bpm_change (bpm)

    bpm change

    Parameters:

    -   bpm

 add_clock_params ()
    add clock params

 enable_midi ()
    enable midi

 process_midi (data)

    process midi

    Parameters:

    -   data

generated by LDoc 1.4.6 Last updated 2023-03-17 18:54:24
vim:tw=78:ts=8:ft=help:norl:
